<h1>Create a Song</h1>

<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br />

  <%= f.text_field :artist_name %>
  <%= f.collection_select :genre_id, Genre.all, :id, :name %>
  <input name='song[note_contents][]' id='song_notes_1'>
  <input name='song[note_contents][]' id='song_notes_2'>
  <input name='song[note_contents][]' id='song_notes_3'>

  <!--

  <%= f.text_field :artist_name, list: "artists_autocomplete" %>
  <datalist id="artists_autocomplete">
    <% @artists.each do |artist| %>
      <option value="<%= artist.name %>">
    <% end %>
  </datalist>

  <%= f.text_field :genre_name, list: "genres_autocomplete" %>
  <datalist id="genres_autocomplete">
    <% @genres.each do |genre| %>
      <option value="<%= genre.name %>">
    <% end %>
  </datalist>

  <%= f.text_field :note_contents, list: "notes_autocomplete" %>
  <datalist id="notes_autocomplete">
    <% @notes.each do |note| %>
      <option value="<%= note %>">
    <% end %>
  </datalist>

  -->

  <%= f.submit %>
<% end %>


