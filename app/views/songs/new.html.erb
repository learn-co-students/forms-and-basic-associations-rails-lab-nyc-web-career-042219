<h1>Create a Song</h1>

<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

	<%= f.label :artist_name %>
  <%= f.text_field :artist_name %>
  <br />

	<%= f.label :genre_id %>
  <%= f.collection_select :genre_id, @genres, :id, :name %>
  <br />


	<input name='song[note_contents][]' id='song_notes_1'>
	<input name='song[note_contents][]' id='song_notes_2'>
	<input name='song[note_contents][]' id='song_notes_3'>
	<br />

	<!-- does not work -->
	<%#= fields_for @song.note_contents do |note| %>
		<%#= note.text_field :contents_1 %>
		<%#= note.text_field :contents_2 %>
		<%#= note.text_field :contents_3 %>
	<%# end %>
	<br />

  <%= f.submit %>
<% end %>
